#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {

  pid_t pid = fork();

  if (pid == 0) {
    printf("I am the child. My PID = %d\n", getpid() );

  } else {
    printf("I am the parent. My PID = %d\n", getpid() );
  }

  return 0;
}
